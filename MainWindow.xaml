<Window x:Class="WindowsNotifier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:WindowsNotifier.Controls"
        mc:Ignorable="d"
        Title="Overlay Notificador de Trading" 
        Height="360" Width="430"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        Closed="Window_Closed">


    <!-- Animaciones globales para la entrada de las alertas -->
    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="0.95" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,50,0,0" To="0,0,0,0" Duration="0:0:0.4">
                <ThicknessAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>

        </Storyboard>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.95" To="0.0" Duration="0:0:0.5"/>
        </Storyboard>
    </Window.Resources>

    <!-- Contenedor Principal (Tarjeta Estilo Cristal/Glassmorphism) -->
    <Border x:Name="NotificationCard" CornerRadius="12" Margin="10" Opacity="0">
        <Border.Background>
            <SolidColorBrush Color="#18181B" Opacity="0.92"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Encabezado / Estado Superior -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="StatusIndicator" Width="10" Height="10" CornerRadius="5" Background="#FFFFCC00" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TitleText" Text="NUEVA ORDEN COLOCADA" Foreground="#FFFFFF" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>

                </StackPanel>
                <TextBlock x:Name="InstanceText" Text="INSTANCIA #104" Foreground="#888888" FontSize="10" Grid.Column="1" VerticalAlignment="Center"/>
            </Grid>

            <!-- Razón de Colocación -->
            <TextBlock x:Name="ReasonText" Text="Razón: Cruce alcista de medias móviles (MA)" Foreground="#DDDDDD" FontSize="12.5" Grid.Row="1" Margin="0,0,0,10" TextWrapping="Wrap" FontStyle="Italic"/>

            <!-- Ganancia Estimada (TP) -->
            <Border x:Name="ProfitContainer" Grid.Row="2" Background="#1A2E26" BorderBrush="#3300FFCC" BorderThickness="1" CornerRadius="6" Padding="8,6" Margin="0,0,0,10" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Ganancia Estimada al TP:" Foreground="#00FFCC" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="EstimatedProfitText" Text="+$125.40 USD" Foreground="#00FFCC" FontWeight="Bold" FontSize="14" Grid.Column="1" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Visualizador de Posición Anterior vs Nueva -->
            <controls:PositionTransitionControl x:Name="PositionVisualizer" Grid.Row="3" VerticalAlignment="Bottom"/>
        </Grid>
    </Border>
</Window>
